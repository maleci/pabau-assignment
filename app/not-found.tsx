import NotFound from "@/components/not-found-page";

export default function NotFoundPage() {
    return (
        <NotFound message="The page you are looking for does not exist."></NotFound>
    );
}